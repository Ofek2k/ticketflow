# TicketFlow · מדריך משתמש ויכולות מערכת

מסמך עזר לצוות הבדיקות — **מה אפשר לעשות בפועל, איך משתמשים יעבדו, ועל מה לשים דגש**.

---

## 1. מי משתמש במערכת ולמה

המערכת משרתת ארבעה קהלים, כל אחד עם דפוס שימוש שונה:

### 1.1 חמ"ל / משתמש קצה (`HAMAL` / `USER`)
**מה הוא בא לעשות:** לפתוח תקלה מהר ולעקוב אחריה.
**איך הוא מתנהג בפועל:** נכנס למערכת, מקליק "תקלה חדשה", ממלא 3 שלבים, ומחכה להודעה כשמטפלים. בדרך כלל לא נשאר במערכת זמן רב — נכנס, פותח, יוצא.
**מה הוא רואה:** רק את התקלות שלו ואת תקלות יחידתו. אין לו דשבורד, אין אנליטיקה.

### 1.2 חט"פ (`HATAP`)
**מה הוא בא לעשות:** לקלוט תקלות מחמ"לים, לטפל במה שיכול, ולהעביר את מה שלא יכול למש"א.
**איך הוא מתנהג בפועל:** רענון תכוף של רשימת תקלות יחידתו, פתיחת תקלה אחת בכל פעם, עדכון סטטוס, סגירה. בעיקר שעות פעילות יחידה.
**מה הוא רואה:** תקלות שמיועדות אליו + של היחידה שלו. רואה גם כפתורי עדכון סטטוס.

### 1.3 בעל תפקיד תפעולי (`HR` / `MAMRAM` / `APP` / `KHOZI`)
**מה הוא בא לעשות:** לטפל בתקלות שמשויכות לתפקידו (מחשבים, רשת, אפליקציות, חוזים).
**איך הוא מתנהג בפועל:** רץ ברשימת התקלות שלו, מסמן "בטיפול", מעדכן ב-comments, סוגר.
**מה הוא רואה:** תקלות שמשויכות לתפקידו. אם הוא גם `is_manager` — רואה גם תקלות של כל היחידה שתחתיו.

### 1.4 מפקד / בעלים (`is_manager` / `OWNER`)
**מה הוא בא לעשות:** תמונת מצב, ניהול אנשים, אישור בקשות.
**איך הוא מתנהג בפועל:** דשבורד, רשימות, אנליטיקה, אישורי תפקיד. הרבה צפייה, פחות פעולה.
**מה הוא רואה:** הכל ביחידה שלו (מפקד) או הכל במערכת (OWNER) — כולל ניהול משתמשים.

---

## 2. היכולות העיקריות — מה אפשר לעשות

### יכולת A — פתיחת תקלה
מה זה עושה: יוצר רשומה חדשה ב‑`tickets`, מנתב למטפל, שולח התראה.
מי יכול: כל משתמש מחובר ומאושר.
איך עושים:
1. קליק "תקלה חדשה" (בסיידבר / בFAB במובייל / בTopbar).
2. **שלב 1**: בוחר נושא (חובה) + אם זה חירום מסמן "חמ"ל מושבת".
3. **שלב 2**: בוחר יחידה ותפקיד מטפל.
4. **שלב 3**: כותרת קצרה (חובה), תיאור מפורט (אופציונלי), קבצים מצורפים (אופציונלי).
5. קליק "פתח תקלה" → מועבר למסך הפרטים של התקלה שנפתחה.

### יכולת B — צפייה ברשימת תקלות
מה זה עושה: מציג טבלה/רשימה של תקלות עם פילטרים.
מי יכול: כל משתמש (רואה רק מה שמותר לו).
פילטרים זמינים:
- חיפוש חופשי (כותרת או מספר תקלה).
- סינון לפי נושא (מחשב / רשת / אפליקציה / אמצעים / עמדה דלה / אחר).
- סינון לפי תפקיד מטפל.
- "פתוחות" / "ארכיון".

### יכולת C — טיפול בתקלה
מה זה עושה: עדכון סטטוס, הוספת תגובות, סגירה.
מי יכול: המטפל המוקצה, מנהלים, OWNER.
פעולות זמינות:
- שינוי סטטוס: פתוח → בטיפול → טופל → סגור.
- הוספת תגובה (chat-style, נראית לכולם בזמן אמת).
- סגירת תקלה (רק הפותח או OWNER).
- צפייה בקבצים מצורפים (לחיצה על תמונה פותחת בטאב חדש; וידאו מתנגן במקום).

### יכולת D — דשבורד בזמן אמת
מה זה עושה: תמונת מצב חיה ליחידה.
מי יכול: מנהלים + OWNER.
מה רואים:
- כרטיס "Hero" שמדגיש תקלת חמ"ל פעילה (אם אין — מציג "שקט מבצעי").
- 3 כרטיסי סטטיסטיקה: שלי / סה"כ פתוחות / טופלו.
- רשימת 6 העדכונים האחרונים (קליק → קופץ ישר לפרטי תקלה).

### יכולת E — אישורי תפקיד
מה זה עושה: מנהל מאשר/דוחה בקשה של משתמש לשדרג תפקיד או להפוך למפקד.
מי יכול:
- **OWNER** — מאשר כל בקשה.
- **מפקד תפקיד** (`is_manager=true`) — מאשר רק בקשות **לתפקידו** (לדוגמה מפקד מש"א מאשר בקשות להיות מש"א).

איך זה זורם:
1. משתמש חדש נרשם → נכנס כ-`HAMAL` עם `role_status=Pending`.
2. הוא או מנהל אחר משדרגים אותו → `requested_role=HR` (לדוגמה).
3. בסיידבר של המפקד מופיע badge עם מספר הבקשות.
4. הוא נכנס ל"אישורי תפקיד", רואה את הבקשה, מקליק "אשר".
5. המשתמש מקבל את התפקיד החדש מיידית.

### יכולת F — ניהול משתמשים
מה זה עושה: רשימה מלאה של משתמשי המערכת + הקפאה.
מי יכול: OWNER בלבד.
פעולות:
- צפייה בכל המשתמשים עם תפקיד, יחידה, סטטוס.
- הקפאת חשבון → המשתמש מנותק מיידית בכניסה הבאה ומקבל הודעה.
- ביטול הקפאה.

### יכולת G — אנליטיקה
מה זה עושה: סיכומים סטטיסטיים על פעילות המערכת.
מי יכול: מנהלים + OWNER.
מה רואים:
- 4 כרטיסי סיכום (פתוחות / טופלו / חמ"ל / בטיפול).
- פילוח לפי נושא (איזה סוגי תקלות הכי נפוצים).
- פילוח לפי תפקיד מטפל (איזה תפקיד עמוס).

### יכולת H — פרופיל אישי
- עדכון שם וטלפון.
- צפייה באימייל (לא ניתן לשינוי).
- התנתקות.

### יכולת I — Realtime
המערכת כל הזמן מסונכרנת:
- תקלה חדשה שנפתחת ביחידה → כל מי שצופה רואה אותה מופיעה תוך שנייה + toast.
- תגובה חדשה במסך פרטי תקלה → מופיעה אצל כל מי שצופה באותה תקלה.
- שינוי סטטוס → מתעדכן אצל כולם.

---

## 3. מסלולי שימוש טיפוסיים (User Journeys)

### מסלול 1: יום בחיים של חמ"ל
1. מקבל פניה מחייל: "המקרן בכיתה 4 לא דולק".
2. נכנס למערכת מהטלפון.
3. FAB → "תקלה חדשה" → נושא "מחשב" → יחידה "פלוגה ב'" → מטפל "חט"פ" → כותרת "מקרן בכיתה 4 לא דולק" → מצרף תמונה → "פתח תקלה".
4. רואה את התקלה במסך פרטים עם סטטוס "פתוח".
5. במהלך היום מקבל הודעת push: "התקלה שלך עברה לסטטוס בטיפול".
6. בערב מקבל הודעה "התקלה טופלה" — נכנס, מוודא, סוגר.

### מסלול 2: יום בחיים של ממר"ם
1. נכנס בבוקר, רואה ב"התקלות שלי" 7 תקלות פתוחות.
2. ממיין לפי דחיפות (חמ"ל למעלה, ישנות אחר כך).
3. פותח תקלה → מקליק "בטיפול".
4. כותב comment "מעלה לכם UPS חדש, אהיה אצלכם תוך שעה".
5. נוסע, מטפל, חוזר, מקליק "טופל".
6. הפותח מקבל push ויכול לסגור.

### מסלול 3: יום בחיים של מ"פ (מפקד)
1. בוקר — נכנס לדשבורד. רואה Hero card עם תקלת חמ"ל פעילה ביחידתו.
2. מקליק → רואה פרטים, מוודא שמישהו מטפל.
3. עובר ל"כל התקלות" → רואה תמונת מצב מלאה של הפלוגה.
4. עובר לאנליטיקה → מבחין שיש זינוק בתקלות "רשת" השבוע — מסמן לעצמו לעלות לדיון.
5. לאחר הצהריים מקבל badge בסיידבר "אישורי תפקיד (1)" — נכנס, מאשר חיילת חדשה כמש"א.

### מסלול 4: יום בחיים של OWNER
1. נכנס בבוקר.
2. דשבורד → לא קורה משהו דחוף.
3. "משתמשים" → בודק אם יש בקשות חדשות. מקפיא חשבון של חייל ששוחרר.
4. "אישורי תפקיד" → מאשר בקשה ממפקד פלוגה להפוך למפקד תפקיד.
5. אנליטיקה לסיום השבוע → מייצא נתונים לדו"ח (ידני — אין export אוטומטי).

---

## 4. על מה לשים דגש בבדיקות

### 4.1 דגשים בעדיפות גבוהה ⭐⭐⭐

**A. הרשאות (Role-Based Access)**
המערכת מסננת תקלות לפי תפקיד ויחידה. **הדבר הכי קל לפספס הוא דליפת מידע**. ספציפית:
- ✓ ודא ש‑`HAMAL` לא רואה תקלות של יחידה אחרת — **גם לא דרך URL ישיר** (`?ticket=1234`).
- ✓ ודא שמשתמש לא-מנהל לא יכול לגשת ל-`MANAGE_USERS` / `ROLE_APPROVAL` גם דרך מניפולציה.
- ✓ ודא שמפקד תפקיד X **לא** יכול לאשר בקשה לתפקיד Y, גם דרך devtools.
- ✓ ודא שגם אם UI הציג בטעות כפתור, RLS חוסם את הפעולה בפועל.

**B. חמ"ל מושבת**
זו הפיצ'ר הכי קריטי תפעולית. בדוק:
- ✓ פתיחת תקלה עם חמ"ל מושבת → push יוצא עם כותרת מיוחדת לכל בעלי התפקידים התפעוליים.
- ✓ התקלה מופיעה ב‑Hero card בדשבורד עם הדגשה אדומה.
- ✓ אנימציית pulse פעילה.
- ✓ משתמש לא-תפעולי שצופה בתקלת חמ"ל לא רואה כפתורי פעולה.

**C. Realtime**
זה הציפייה של המשתמשים מהמערכת — שדברים מתעדכנים מיד.
- ✓ פתח שני דפדפנים, באחד פתח תקלה, בשני בדוק שמופיעה תוך שנייה.
- ✓ הוסף תגובה במסך פרטים → השני רואה אותה מיד.
- ✓ אם המשתמש באמצע מילוי אשף (`CREATE`), אל תרענן לו את הדף ב-realtime.
- ✓ סגירת תקלה אצל אחד → השני רואה את הסטטוס מתעדכן.

**D. מובייל (≤900px)**
חצי מהמשתמשים יגיעו מהטלפון.
- ✓ Bottom nav במקום sidebar.
- ✓ FAB לפתיחת תקלה מהיר.
- ✓ Inputs ב‑16px לפחות (אחרת iOS עושה auto-zoom).
- ✓ Pull-to-refresh עובד.
- ✓ Safe areas של iPhone X+ מכובדות.

### 4.2 דגשים בעדיפות בינונית ⭐⭐

**E. אשף יצירת תקלה**
זו ה-flow הכי מורכב.
- ✓ דילוג קדימה בלי לבחור → toast מתאים, לא ממשיך.
- ✓ חזרה אחורה שומרת ערכים.
- ✓ ביטול מאפס הכל.
- ✓ העלאת קבצים גדולים — לבדוק עד איזה גודל זה עובד.
- ✓ סוגי קבצים מותרים: תמונה / וידאו בלבד.

**F. חיפוש ופילטרים**
- ✓ חיפוש לא רגיש לאותיות.
- ✓ חיפוש לפי מספר תקלה (גם חלקי) עובד.
- ✓ פילטרים משולבים (נושא + תפקיד) עובדים יחד.
- ✓ "איפוס סינון" מציג שוב את כל הרשימה.
- ✓ debounce של 250ms — לא מציף את ה-DB בכל הקשה.

**G. תפקידים מרובים ביחידה**
- ✓ משתמש עם מספר יחידות במערך → רואה תקלות מכולן.
- ✓ Sub-units (יחידות-בנות עם `parent_unit`) — מנהל רואה גם אותן.
- ✓ יחידה בודדת → לא מציג select מיותר באשף.

**H. אישורי תפקיד (חדש)**
- ✓ Badge בסיידבר מציג מספר מדויק.
- ✓ מפקד HR רואה רק בקשות `requested_role=HR`.
- ✓ אישור מעדכן `role`, `is_manager`, ומאפס בקשות.
- ✓ דחייה לא משנה תפקיד, רק מסמן Rejected.

### 4.3 דגשים בעדיפות נמוכה ⭐

**I. UX קטנים**
- Toasts נעלמים אחרי 3 שניות.
- Modal נסגר בקליק מחוץ.
- אנימציות עמוד חלקות, בלי קפיצות.
- אייקונים נטענים גם בחיבור איטי.

**J. PWA**
- אפשר להתקין כאפליקציה.
- Service Worker מתעדכן בלי לשבור session.

**K. נראות (Aurora)**
- צבעי הגרדיאנט תקינים.
- Sparklines מציגים גרף ולא ריבועים ריקים.
- כרטיסי Bento ב-grid תקין בכל הרזולוציות.

---

## 5. בעיות נפוצות שכדאי לחפש

### 5.1 דברים שאני יודע ששווה לבדוק (מהקוד):
- `getNextNum()` רץ ב-client side — מה קורה אם שניים פותחים תקלה בו זמנית? יכול להיווצר תקלות עם אותו `ticket_number`.
- אין rate limit על תגובות — אפשר להציף chat.
- `comments` נטענים רק כשנכנסים למסך הפרטים — אם נכנסים, יוצאים וחוזרים, יש דקה של "אין תגובות" עד שטוען.
- Push Worker דורש HTTPS — בסביבת dev מקומית התראות לא יעבדו.
- אם יחידה במסד מכילה פסיק בשם, ה-`split(',')` ישבור.
- אם משתמש עם תפקיד מסוים מוקפץ דרך devtools להיות OWNER ב-localStorage / state → ה-UI יציג לו אופציות, אבל RLS אמור לחסום בפועל. **בדוק שזה אכן חוסם**.

### 5.2 קצוות נתונים (Edge data) שכדאי לנסות:
- כותרת תקלה עם 200 תווים.
- כותרת עם אמוג'י, סימני פיסוק עברית-אנגלית מעורבים, גרשיים בודדים וכפולים.
- שם משתמש ריק / רק רווחים.
- יחידה עם תווים מיוחדים: `"חמ\"ל ב'"`.
- תקלה ללא תיאור.
- תקלה עם 10 קבצים מצורפים.
- מספר תקלה גבוה (>99999).
- שעון מחשב לא מסונכרן (TimeAgo יכול להראות "לפני −5ד'").

### 5.3 תרחישים מבצעיים אמיתיים:
- **גל תקלות** — 20 תקלות נפתחות תוך 5 דקות. האם הרשימה והדשבורד עומדים בעומס?
- **לילה ללא רשת** — משתמש פותח את האפליקציה אחרי שאיבד רשת. מה הוא רואה?
- **התחלפות תפקיד באמצע סשן** — OWNER משנה את התפקיד של משתמש מחובר. מה קורה אצלו?
- **מחיקת משתמש** — מה קורה לתקלות שהוא פתח? (`created_by_name` נשאר כ-snapshot, אבל `created_by_user_id` יוצר orphan).

---

## 6. דברים שהמערכת **לא** עושה (מגבלות מודעות)

חשוב לתאם ציפיות עם הצוות:

- ❌ אין הקצאה אישית — תקלה משויכת ל-**תפקיד** ולא לאדם ספציפי.
- ❌ אין export ל-Excel / PDF.
- ❌ אין SLA מובנה (יש "תקלה ישנה מעל שעה" וזה הכל).
- ❌ אין שיחה פנימית בין מטפלים מחוץ ל-comment של תקלה.
- ❌ אין notifications שמתפרסות בין מכשירים — Push מצריך הרשמה לכל מכשיר בנפרד.
- ❌ אין מצב כהה ל-Aurora (העיצוב בהיר במכוון).
- ❌ אין undo לאחר סגירת תקלה (אבל אפשר לפתוח מחדש כתקלה חוזרת).
- ❌ אין היסטוריית עריכה של כותרת/תיאור (אי אפשר לערוך אחרי פתיחה).

---

## 7. סדר עדיפויות מומלץ לבדיקות

אם יש זמן מוגבל, עברו לפי הסדר הזה:

1. **Auth + onboarding** (~30ד') — בלי זה אי אפשר להגיע לכלום.
2. **פתיחת תקלה + צפייה בה** (~45ד') — ה-core flow.
3. **הרשאות** (~60ד') — הסיכון הכי גדול.
4. **חמ"ל מושבת** (~20ד') — הפיצ'ר הקריטי תפעולית.
5. **Realtime** (~30ד') — צריך שני דפדפנים.
6. **מובייל** (~45ד') — הכי הרבה משתמשים שם.
7. **אישורי תפקיד** (~20ד') — הפיצ'ר החדש.
8. **רשימות ופילטרים** (~30ד') — לא קריטי אבל מורגש.
9. **אנליטיקה + ניהול משתמשים** (~20ד') — OWNER בלבד, פחות שימוש.
10. **PWA + Push** (~30ד') — דורש HTTPS אמיתי, לעיתים מתעכב לבסוף.

**סה"כ:** סבב מהיר ~5 שעות, סבב יסודי 8-10 שעות.

---

*לכל שאלה — פנו ליצרן המערכת.*
